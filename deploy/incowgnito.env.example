# Incowgnito — app-specific environment variables
# Place as: /opt/mailcow-dockerized/incowgnito.env
#
# DB credentials (DBNAME, DBUSER, DBPASS) and MAILCOW_HOSTNAME
# are inherited from mailcow.conf via docker-compose env_file.

MAILCOW_API_KEY=your-mailcow-admin-api-key
MAILCOW_OAUTH_CLIENT_ID=your-oauth-client-id
MAILCOW_OAUTH_CLIENT_SECRET=your-oauth-client-secret
RELAY_DOMAIN=alias.example.com
APP_URL=https://relay.example.com
SESSION_SECRET=replace-with-openssl-rand-hex-32
